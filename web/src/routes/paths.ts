export const PATH_AUTH = {
  login: '/login',
  register: '/register',
  resetPassword: '/reset-password'
}

export const PATH_COMMON = {
  home: '/'
}

export const PATH_USERS = {
  settings: '/settings',
  profile: '/:username'
}
