import { createTheme } from '@material-ui/core';

const theme = createTheme({
  typography: {
    h3: {
      fontWeight: 600
    },
    h4: {
      fontWeight: 600
    },
    h5: {
      fontWeight: 600
    }
  }
});

export default theme;
